{
  "name": "nuga-skin-clinic",
  "version": "1.0.0",
  "description": "Nuga Skin Clinic — official website (React + Vite + TS client, Express server)",
  "private": true,
  "scripts": {
    "install:client": "cd client && npm install",
    "build": "npm run install:client && cd client && npm run build",
    "start": "node server/index.js",
    "dev": "cd client && npm run dev"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
